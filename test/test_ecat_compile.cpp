// OBSOLETE -- this tested the old EcatDrives HAL, which has been removed.
// The EtherCAT layer is now apps/ecat_control_node.cpp (ported from main.cpp). To compile-
// check it offline against the simulated-drive mock, run:  make test-ecat-mock
int main() { return 0; }
