# erobo10 EtherCAT runtime calibration -- TEMPLATE (safe defaults == ecat_config.hpp).
# Auto-loaded by EcatDrives if present. As-is it changes nothing. Run bring-up to populate:
#   1) ./control_node --hardware --selftest --allow-motion   (verify direction signs)
#   2) edit dir=... below for any joint that moved the wrong way vs the URDF
#   3) drive/teach to a known reference pose, then:
#      ./control_node --hardware --set-home <q0..q5>          (writes offset=... below)
#   4) if a brake clicks the wrong way, swap brake_release / brake_apply
# (Control gains -- ANGLE_KP, MIN_VEL, DEADBAND, SPEED_LIMIT -- live in ecat_config.hpp.)
dir=1,1,1,1,1,1
offset=0,0,0,0,0,0
cpr=524287,524287,524287,524287,524287,524287
brake_release=1
brake_apply=0
